import React, { Fragment } from 'react';

function Form(props) {
    const {
        onChangeModel,
        onSubmitModel,
        onClickGoBack,
    } = props;

    function handleClickColors(event) {

        event.target.after(addColors());
    }


        );
    }

  return (

  );
}

export default Form;